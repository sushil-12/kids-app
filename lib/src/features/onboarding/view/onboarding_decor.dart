/// The pastel-clay decor now lives in `core/widgets/clay_decor.dart` so every
/// feature can use it; this re-export keeps onboarding imports working.
library;

export '../../../core/widgets/clay_decor.dart';
